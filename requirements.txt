fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
python-dotenv==1.0.1
PyYAML==6.0.2

# HTTP clients
httpx==0.27.2
aiohttp==3.10.8
aiofiles==24.1.0
backoff==2.2.1
websockets==13.1

# Cache / queue
redis[hiredis]==5.1.0

# Storage
boto3==1.35.36

# Image / audio
Pillow==10.4.0
pydub==0.25.1

# Observability
prometheus-client==0.21.0
structlog==24.4.0

# Security
PyJWT==2.9.0
passlib[bcrypt]==1.7.4

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# --- Part 3.1 intent service: optional ML backend -------------------------
# Intent service ships a zero-dep rule-based classifier by default. To enable
# the transformer backend, install the extras below (CPU-only wheels shown).
# torch==2.4.1
# transformers==4.45.2
# sentencepiece==0.2.0

# --- Part 3.2 decision engine: optional ML strategy ----------------------
# Decision engine ships with a zero-dep ``dummy`` model. To use a trained
# scikit-learn classifier, install the extras below and set
# DECISION_ML_MODEL_KIND=sklearn + DECISION_ML_MODEL_PATH=/path/to/model.joblib
# scikit-learn==1.5.2
# joblib==1.4.2
